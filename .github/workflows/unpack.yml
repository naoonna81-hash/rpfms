{"name":"Unpack source","on":{"push":{"branches":["main"]},"workflow_dispatch":{}},"permissions":{"contents":"write"},"jobs":{"unpack":{"runs-on":"ubuntu-latest","steps":[{"uses":"actions/checkout@v4"},{"name":"Unzip source if present","run":"if [ -f RPFMS-source.zip ]; then unzip -o RPFMS-source.zip -d .; rm -f RPFMS-source.zip; git config user.name github-actions[bot]; git config user.email github-actions[bot]@users.noreply.github.com; git add -A; git diff --cached --quiet || git commit -m 'Unpack RPFMS-source.zip into repository'; git push; else echo 'No zip found, nothing to unpack.'; fi"}]}}}
